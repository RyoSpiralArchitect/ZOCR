# Boarding Pass Samples / 搭乗券サンプル / Cartes d'embarquement

- **JA:** `--domain boarding_pass` を指定して搭乗券PDF/PNGを配置すると、便名・座席・搭乗時刻を抽出してSQL/RAGに流します。
- **EN:** Use `--domain boarding_pass`; flight number, seat and gate details are exported for agents that orchestrate travel flows.
- **FR:** Lancez avec `--domain boarding_pass` ; numéro de vol, siège et porte sont extraits pour vos agents RAG.
