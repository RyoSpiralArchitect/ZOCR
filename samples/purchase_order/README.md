# Purchase Order Samples / 発注書サンプル / Bons de commande

- **JA:** `--domain purchase_order` を指定し、発注書やPOのPDF/PNGを入れてください。列ヘッダ（Item/Qty/Unit Price/Amount）は自動補完されます。
- **EN:** Use `--domain purchase_order` when running the pipeline; headers such as Item/Qty/Unit Price/Amount are reinforced automatically.
- **FR:** Lancez avec `--domain purchase_order` ; les en-têtes (Item/Qty/Unit Price/Amount) sont complétés automatiquement.
