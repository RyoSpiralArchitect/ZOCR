apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - pvc.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml

# Optional secret for API key:
#   kubectl create secret generic zocr-api-secret --from-literal=ZOCR_API_KEY=change-me

